{
  "version": "1.0.0",
  "name": "jia",
  "private": true,
  "scripts": {
    "build": "npx lerna run build --parallel"
  },
  "engines": {
    "node": ">=14"
  },
  "workspaces": [
    "packages/demo"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/jia000/action-test.git"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
